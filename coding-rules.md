Luôn dùng TypeScript.

Không dùng JavaScript.

Không dùng any.

Luôn viết component nhỏ.

Một component chỉ có một nhiệm vụ.

Không viết file trên 300 dòng.

Ưu tiên hooks.

Ưu tiên async await.

Không dùng CSS thuần.

Chỉ dùng Tailwind.

Mọi API phải có validation.

Mọi API phải có try catch.

Tên biến dùng tiếng Anh.

Tên component PascalCase.

Tên hook camelCase.

Tên file đúng chuẩn React.

Không sử dụng Docker bắt buộc.

Docker chỉ là tùy chọn.

Ứng dụng phải chạy trực tiếp bằng NodeJS.

Không dùng package không hỗ trợ ARM64.

Không phụ thuộc Windows.

Đường dẫn phải tương thích Linux.

Không hardcode ổ đĩa C:.

Không dùng Batch.

Chỉ dùng npm.

PM2 quản lý tiến trình.

Mọi cấu hình nằm trong file .env.

Không được commit code chưa build.

Không kết thúc task nếu build lỗi.

Sau mỗi thay đổi phải chạy project.

Nếu phát hiện lỗi phải tự sửa.

Không được yêu cầu người dùng test thay AI nếu có thể tự kiểm thử trong môi trường của AI.